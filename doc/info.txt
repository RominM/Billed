
ERREURS IDENTIFIE:

/**
 * @jest-environment jsdom
 */

// src/views/BillsUI.js
      => rows
         => add .sort() methode to get date in good ordre
         

// src/containers/Login.js
      => handleSubmitAdmin
         => email & password querySelector the wrong input[data-testid]

